// Task 1
// Select an HTML element by its ID and change its text content.
const selectedById = document.getElementById("sample-id");
selectedById.textContent = "I'm superior!!!";

// Task 2
// Select an HTML element by its class and change its background color.
const selectedByClass = document.querySelector(".sample-class");
selectedByClass.style.backgroundColor = "red";
